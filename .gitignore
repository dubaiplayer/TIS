# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# Virtual environment
.venv/
venv/

# Data & model artifacts — large and regenerable (see data_prep.py / classifier.py)
data/
models/
reports/
# ...except the trained classifier, which the hosted service needs at runtime:
!models/
!models/classifier.joblib

# Node / Electron (desktop app)
desktop/node_modules/
desktop/dist/
desktop/release/

# OS
Thumbs.db
.DS_Store

models/classifier_baseline.joblib

extension/key.pem
extension/.manifest_key.txt

# Store upload package — regenerate with scripts/build_extension_zip.py
inbox-shield.zip

# Built store packages — regenerate with scripts/build_extension_zip.py
dist/
